apply plugin: 'jacoco'
apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.antlr:antlr4-runtime:4.5'
    compile 'org.antlr:stringtemplate:4.0.2'

    compile 'commons-io:commons-io:2.4'

    testCompile 'com.github.stefanbirkner:system-rules:1.12.1'
    testCompile 'junit:junit:4.12'
}

jacoco {
    toolVersion = '0.7.5.201505241946'
}

jacocoTestReport {
    reports {
        xml.enabled = true
        html.enabled = true
    }
}

task generate(type: JavaExec) {
    description 'Generates Java bytecode: -Psrc filename'
    main = 'tours.compiler.Compiler'
    classpath = sourceSets.main.runtimeClasspath
    if (project.hasProperty('src')) {
        args project.src
    }
}

task generateAndSave(type: JavaExec) {
    description 'Generates Java bytecode and save it a directory: -Psrc filename -Pdir directory'
    main = 'tours.compiler.Compiler'
    classpath = sourceSets.main.runtimeClasspath
    if (project.hasProperty('src') && project.hasProperty('dir')) {
        args (project.src, '--save', project.dir)
    }
}

task execute(type: JavaExec) {
    description 'Executes: -Psrc filename'
    main = 'tours.compiler.Compiler'
    classpath = sourceSets.main.runtimeClasspath
    if (project.hasProperty('src')) {
        args (project.src, '--execute', '--no-bytecode')
    }
}
