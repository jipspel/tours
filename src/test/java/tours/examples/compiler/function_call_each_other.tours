func main() {
    integer i = 34;
    printInteger(34);
}

func void printInteger(integer i) {
    while( i < 36) {
        print(i);
        i = addOneWithPrint(i);
    }
}

func integer addOneWithPrint(integer i) {
    printInteger(i+1);
    return i + 1;
}
