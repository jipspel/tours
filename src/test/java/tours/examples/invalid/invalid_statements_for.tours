main() {
    integer x;
    integer i;
       // invalid condition
    for (x = 1; -x; x = x + 1) {
        i = 35;
    }

    // invalid condition
    for (x = 1; x + x; x = x + 1) {
        i = 35;
    }

    // invalid condition
    for (x = 1; x * x; x = x + 1) {
        i = 35;
    }

    // invalid condition
    for (x = 1; 35; x = x + 1) {
        i = 35;
    }

    // invalid condition
    for (x = 1; "Hello"; x = x + 1) {
        i = 35;
    }
}
