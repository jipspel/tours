func main() {
    integer x;
    boolean y;
    character z;
    string a;

    x = 35;
    y = false;
    z = 'a';
    a = "hello";

    x = - 3;
    x = 3 + 3;
    x = 3 - 3;
    x = 3 * 3;
    x = 3 / 3;
    x = 3 % 3;

    y = !false;
    y = true && true;
    y = 2 > 1;

    y = 1 == 1;
    y = 'a' == 'a';
    y = true == true;

    y = 1 != 1;
    y = 'a' != 'a';
    y = true != true;

    x = print(3);
    y = print(false);
    z = print('a');
    a = print("Hello!");
}
