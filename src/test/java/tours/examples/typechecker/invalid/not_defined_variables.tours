func main() {
    integer x;
    y = 35;
    boolean[] b = new boolean[5];
    c[0];

    scope();
    z;

    i = addOneWithArgument(35);

    do_nothing(33);
    j;

    input(k);
    print(l);
}

func void add_one() {
    35 + x;
    x + 35;

    35 - x;
    x - 35;

    35 * x;
    x * 35;

    35 / x;
    x / 35;
}

func void scope() {
    integer z = 3;
}

func integer add_one_with_argument(integer i) {
    return i + 1;
}

func void do_nothing(integer j) {}
