func void main() {
    integer x;
    integer i;
    for (x = 1; -x; x = x + 1) {
        i = 35;
    }

    for (x = 1; x + x; x = x + 1) {
        i = 35;
    }

    for (x = 1; x * x; x = x + 1) {
        i = 35;
    }

    for (x = 1; 35; x = x + 1) {
        i = 35;
    }

    for (x = 1; "Hello"; x = x + 1) {
        i = 35;
    }
}
