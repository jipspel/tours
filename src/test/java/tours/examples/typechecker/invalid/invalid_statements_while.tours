func void main() {
    integer x;
    character y;
    string z;

    while (-x) {}
    while ((x)) {}
    while (x + 35) {}
    while ('x' < 35) {}
    while (y) {}
    while (z) {}
    while ('a') {}
    while (1) {}
    while ("aa") {}
    while (x * x) {}
}
